{% extends 'reset-password/layout.html.twig' %}

{% block body %}
    <div class="card">
        <div class="card-header">
            <h2 class="card-title h4 mb-0">Changez votre mot de passe avec votre adresse mail</h2>
        </div>
        <div class="card-body">
            {% for flashError in app.flashes('reset_password_error') %}
                <div class="alert alert-danger" role="alert">{{ flashError }}</div>
            {% endfor %}

            {{ form_start(requestForm) }}
            {{ form_row(requestForm.email, {'icon_prepend': 'envelope'}) }}
            <div class="text-center">
                <button class="btn btn-raised btn-primary" type="submit">Réinitialiser le mot de passe</button>
            </div>
            {{ form_end(requestForm) }}
        </div>
    </div>
{% endblock %}
