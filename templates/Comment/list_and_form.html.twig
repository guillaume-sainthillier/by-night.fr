<div class="comments">
    <h4 class="heading">
        {{ nb_comments }} Commentaire{{ nb_comments > 1 ? "s" : "" }}
        {% if nb_comments == 0 %}
            |
            <small>Soyez le premier à réagir</small>
        {% endif %}
    </h4>
    <br />
    {% include "Comment/post.html.twig" %}
    {% if nb_comments > 0 %}
        {% include "Comment/list.html.twig" %}
    {% endif %}
</div>