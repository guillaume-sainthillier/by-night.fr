{% import 'form/macros.html.twig' as macros %}
{% for key, messages in app.flashes %}
    {% for message in messages %}
        <div class="alert alert-{{ key == 'error' ? 'danger' : key }}">
            <div class="container-lg">
                {% if key == 'info' %}
                    {{ macros.icon('fa6-solid:circle-info') }}
                {% elseif key == 'error' %}
                    {{ macros.icon('fa6-solid:triangle-exclamation') }}
                {% elseif key == 'success' %}
                    {{ macros.icon('fa6-solid:check') }}
                {% endif %}
                {{ message|trans|raw }}
            </div>
        </div>
    {% endfor %}
{% endfor %}
