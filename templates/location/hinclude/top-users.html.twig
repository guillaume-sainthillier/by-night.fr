{{ include('partials/user/_list-widget.html.twig') }}
{% if hasNextLink is defined and hasNextLink %}
    <a class="more-content btn btn-outline-primary btn-block" href="{{ hasNextLink }}">
        Plus ({{ current }} / {{ count }})</a>
{% endif %}
