<div class="row">
    {% for name, social in shares %}
        <div class="col-6">
            <a href="{{ social.shareUrl }}" class="popup share {{ name }}">
                <i class="fa fa-2x fa-{{ name }}"></i>
                <span class="number number-xbig">Partager</span>
            </a>
        </div>
    {% endfor %}
</div>
