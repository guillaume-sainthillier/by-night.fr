<div class="row">
    {% for user in users %}
        {% include "User/block_details_user.html.twig" %}
    {% endfor %}
</div>

{% if users.hasNextPage %}
    <div class="text-center my-4">
        <a class="btn btn-outline-primary more"
           href="{{ path('app_search_query', {'q' : term, 'page': users.nextPage, 'type' : 'membres'}) }}">
            Plus
        </a>
    </div>
{% endif %}
