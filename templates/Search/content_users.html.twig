<div class="row">
    {% for user in users %}
        {% include "User/block_details_user.html.twig" %}
    {% endfor %}
</div>

{% if users|length == maxItems %}
    <div class="text-center my-4">
        <a class="btn btn-outline-primary more"
           href="{{ path('app_search_query', {'q' : term, 'page': page+1, 'type' : 'membres'}) }}">
            Plus
        </a>
    </div>
{% endif %}
