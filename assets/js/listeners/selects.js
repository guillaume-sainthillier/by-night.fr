export default (di, container) => {
    /*
    $('select', container).each(function () {
        $(this).selectpicker({
            style: $(this).data('style') || 'btn-primary',
        });
    });
     */
};
